<h2 class="page-header">Bands</h2>

<ul class="main-list">
  <% @bands.each do |band| %>
    <li><a href= "<%=band_url(band)%>">
      <p><%= band.name %></p></a>
    </li>
  <%end%>
</ul>

<a href="<%= new_band_url %>">Create New Band</a>
